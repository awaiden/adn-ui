"use client";

import { Surface } from "@adn-ui/react";

const Default = () => (
	<Surface className="p-6">
		<p className="text-sm">This is a surface container with default styling.</p>
	</Surface>
);

export const SurfaceExample = {
	Default,
};
