import type { DemoItem } from "..";

import Default from "./default";

export const numberFieldDemos: Record<string, DemoItem> = {
  "number-field": {
    component: Default,
    file: "number-field/default.tsx",
  },
};
