import type { DemoItem } from "..";

import Default from "./default";

export const containerDemos: Record<string, DemoItem> = {
  container: {
    component: Default,
    file: "container/default.tsx",
  },
};
