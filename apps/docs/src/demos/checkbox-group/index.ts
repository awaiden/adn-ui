import type { DemoItem } from "..";

import Default from "./default";

export const checkboxGroupDemos: Record<string, DemoItem> = {
  "checkbox-group": {
    component: Default,
    file: "checkbox-group/default.tsx",
  },
};
