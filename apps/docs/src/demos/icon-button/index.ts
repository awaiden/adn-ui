import type { DemoItem } from "..";

import Default from "./default";

export const iconButtonDemos: Record<string, DemoItem> = {
  "icon-button": {
    component: Default,
    file: "icon-button/default.tsx",
  },
};
