import type { DemoItem } from "..";

import Default from "./default";

export const navbarDemos: Record<string, DemoItem> = {
  navbar: {
    component: Default,
    file: "navbar/default.tsx",
  },
};
