import type { DemoItem } from "..";

import Default from "./default";

export const toggleButtonDemos: Record<string, DemoItem> = {
  "toggle-button": {
    component: Default,
    file: "toggle-button/default.tsx",
  },
};
