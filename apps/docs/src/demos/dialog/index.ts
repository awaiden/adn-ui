import type { DemoItem } from "..";

import Default from "./default";

export const dialogDemos: Record<string, DemoItem> = {
  dialog: {
    component: Default,
    file: "dialog/default.tsx",
  },
};
