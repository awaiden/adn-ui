import type { DemoItem } from "..";

import Default from "./default";

export const scrollAreaDemos: Record<string, DemoItem> = {
  "scroll-area": {
    component: Default,
    file: "scroll-area/default.tsx",
  },
};
