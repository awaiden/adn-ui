import type { DemoItem } from "..";

import Default from "./default";

export const navigationMenuDemos: Record<string, DemoItem> = {
  "navigation-menu": {
    component: Default,
    file: "navigation-menu/default.tsx",
  },
};
