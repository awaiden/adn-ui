import type { DemoItem } from "..";

import Default from "./default";

export const fieldsetDemos: Record<string, DemoItem> = {
  fieldset: {
    component: Default,
    file: "fieldset/default.tsx",
  },
};
