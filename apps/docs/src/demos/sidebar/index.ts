import type { DemoItem } from "..";

import Default from "./default";

export const sidebarDemos: Record<string, DemoItem> = {
  sidebar: {
    component: Default,
    file: "sidebar/default.tsx",
  },
};
