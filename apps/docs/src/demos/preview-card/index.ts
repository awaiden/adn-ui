import type { DemoItem } from "..";

import Default from "./default";

export const previewCardDemos: Record<string, DemoItem> = {
  "preview-card": {
    component: Default,
    file: "preview-card/default.tsx",
  },
};
