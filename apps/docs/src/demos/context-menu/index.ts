import type { DemoItem } from "..";

import Default from "./default";

export const contextMenuDemos: Record<string, DemoItem> = {
  "context-menu": {
    component: Default,
    file: "context-menu/default.tsx",
  },
};
