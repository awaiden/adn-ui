import type { DemoItem } from "..";

import Default from "./default";

export const comboboxDemos: Record<string, DemoItem> = {
  combobox: {
    component: Default,
    file: "combobox/default.tsx",
  },
};
