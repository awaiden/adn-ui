---
title: Group
description: Wraps an input with a prefix or suffix adornment.
---

import { GroupExample } from "../../../components/GroupExample";
import { ComponentPreview } from "../../../components/ComponentPreview";

Wraps an input with a prefix or suffix adornment.

## Preview

<ComponentPreview>
  <GroupExample.WithPrefix client:load />
</ComponentPreview>

## Usage

```tsx
import { Group, Input } from "@adn-ui/react";

export default () => (
  <Group.Root className="w-full max-w-sm">
    <Group.Prefix>
      <span className="pl-3 text-muted-foreground">@</span>
    </Group.Prefix>
    <Input className="pl-8" placeholder="username" />
  </Group.Root>
);
```

## API Reference

<table className="min-w-full text-left text-sm">
  <thead>
    <tr>
      <th className="py-2 px-4 border-b">Component</th>
      <th className="py-2 px-4 border-b">Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td className="py-2 px-4 border-b">`Group.Root`</td>
      <td className="py-2 px-4 border-b">
        Container that positions the prefix/suffix relative to the input.
      </td>
    </tr>
    <tr>
      <td className="py-2 px-4 border-b">`Group.Prefix`</td>
      <td className="py-2 px-4 border-b">Content placed at the start (left) of the input.</td>
    </tr>
    <tr>
      <td className="py-2 px-4 border-b">`Group.Suffix`</td>
      <td className="py-2 px-4 border-b">Content placed at the end (right) of the input.</td>
    </tr>
  </tbody>
</table>

## Examples

### With Prefix

<ComponentPreview>
  <GroupExample.WithPrefix client:load />
</ComponentPreview>

```tsx
<Group.Root className="w-full max-w-sm">
  <Group.Prefix>
    <span className="pl-3 text-muted-foreground">@</span>
  </Group.Prefix>
  <Input className="pl-8" placeholder="username" />
</Group.Root>
```

### With Suffix

<ComponentPreview>
  <GroupExample.WithSuffix client:load />
</ComponentPreview>

```tsx
<Group.Root className="w-full max-w-sm">
  <Input className="pr-12" placeholder="Search..." />
  <Group.Suffix>
    <span className="pr-3 text-muted-foreground">⌘K</span>
  </Group.Suffix>
</Group.Root>
```

### With Prefix and Suffix

<ComponentPreview>
  <GroupExample.WithPrefixAndSuffix client:load />
</ComponentPreview>

```tsx
<Group.Root className="w-full max-w-sm">
  <Group.Prefix>
    <span className="pl-3 text-muted-foreground">$</span>
  </Group.Prefix>
  <Input className="px-8" placeholder="0.00" type="number" />
  <Group.Suffix>
    <span className="pr-3 text-muted-foreground">USD</span>
  </Group.Suffix>
</Group.Root>
```
